# =================================================
# ========    FLAMEBORN ENV CONFIGURATION    ========
# =================================================

# ======== Network Configuration ========
# RPC endpoint for the Celo Alfajores testnet
ALFAJORES_RPC_URL=https://alfajores-forno.celo-testnet.org

# ======== Operator Wallet ========
# The private key of the wallet that will send transactions (e.g., grant roles, mint tokens).
# IMPORTANT: Never commit a real private key.
PRIVATE_KEY=0xYOUR_PRIVATE_KEY

# ======== Contract Addresses ========
# The address of the deployed FLBToken (ERC20) contract.
FLB_TOKEN_ADDRESS=0x...
# The address of the deployed Engine contract, which receives the minted tokens.
ENGINE_ADDRESS=0x...

# ======== Airdrop Configuration ========
# The total amount of FLB tokens to mint to the Engine for distribution.
MINT_TOTAL_FLB=200

# ======== Optional: Gas Price Overrides ========
# Manually set gas fees to override the automatic fee calculation.
# Useful for pushing through stuck transactions on a congested network.
# All values are in GWEI.
# GAS_MAX_FEE_GWEI=8
# GAS_MAX_PRIORITY_GWEI=3

# ======== Optional: Distribution File ========
# Override the default recipients list location used by engine_distribute_to_all.ts.
# RECIPIENTS_FILE=recipients.json
